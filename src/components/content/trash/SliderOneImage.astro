---
import SingleImageFromPath from '@components/content/SingleImageFromPath.astro';
const {path, height, width, lang, index} = Astro.props;

//////////////////////console.log("pathA");
//////////////////////console.log(path);

---

<div
      class="absolute top-0 left-0 w-full h-full object-cover opacity-0 transition-opacity duration-500 ease-in-out invisible"
      data-slider-container>
	<SingleImageFromPath path={path} height={height} width={width} lang={lang} index={index} />
</div>
